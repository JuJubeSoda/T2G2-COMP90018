<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.unimelb.survey.mapper.SurveyQuestionOptionMapper">
    <select id="countUnqualifiedOption" resultType="Integer" parameterType="Integer">
        SELECT COUNT(1)
        FROM wj_survey_question a,wj_survey_question_option b
        WHERE a.`id` = b.`question_id`
        AND a.`survey_id` = #{surveyId}
        AND b.`option_content` IS NULL
    </select>
</mapper>